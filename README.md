# wallpaper_switcher_for_arch

Add images to wallpaper folder

main file is imageScript.ts

Add exec-once = cd ~/wallpaper_switcher_for_arch && tsc && node imageScript.js to hyprland.conf

# npm
npm build
npm start
