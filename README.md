# wallpaper_switcher_for_arch

Add images to wallpaper folder

main file is imageScript.ts

Add exec-once = cd ~/picture_script && tsc && node imageScript.js to hyprland.conf

# npm
npm build
npm start